# Production Database Configuration
# Get these from: Supabase Dashboard → Settings → Database
# Use "Transaction" mode for DIRECT_URL and "Session" mode for DATABASE_URL (connection pooling)
DATABASE_URL="your-supabase-connection-pooling-url"
DIRECT_URL="your-supabase-direct-url"

# Payload CMS Secret
# Generate a random string: openssl rand -base64 32
PAYLOAD_SECRET="your-payload-secret"

# Google Cloud Storage (Production image uploads)
# Leave unset for local development — Payload uses public/media/ by default.
# When GCS_BUCKET_NAME is set, the GCS storage adapter activates automatically.
GCS_BUCKET_NAME="thenycoptometrist-media"
GCS_PROJECT_ID="your-gcp-project-id"
# Full service account JSON key (single line). Convert with: cat key.json | jq -c .
GCS_CREDENTIALS='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'
